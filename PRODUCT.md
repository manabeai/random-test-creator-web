# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 競技プログラミングの問題作成者。問題の入力構造と制約を組み立て、表示内容と生成されるテストケースを確認する。
- 競技プログラミングの問題を解いている人。取り組んでいる問題の入力構造と制約を表現し、ランダムな入力例を得る。

## Product Purpose

競技プログラミング問題の入力形式と制約を構造化して編集し、その同じ定義から仕様の表示とランダムテストケースを生成できるようにする。問題の作成中にも回答中にも、構造・制約・生成結果の食い違いを減らしながら試行できることを目指す。

## Positioning

入力形式、制約、プレビュー、ランダムサンプルを別々に記述するのではなく、単一の構造化ASTを正本として、編集UI、TeX形式の表示、ランダムサンプル、共有可能な状態へ一貫して投影する。

## Operating Context

- ブラウザ上で入力構造と制約を編集し、結果のプレビューを同じ画面で確認する。
- 編集状態はURLで共有できる。
- 対象問題をAtCoderのABCに限定しない。
- 問題の入力形式と制約を表示するときは、AtCoderの表示形式を維持する。

## Capabilities and Constraints

- スカラー、配列、行列、繰り返し、辺リスト、クエリ列、複数テストケースなどの入力構造を扱う。
- 値域、文字集合、文字列長、総和などの制約を扱う。
- `Tree`／`Connected`／`Simple`などのグラフ性質は、生成意味論と編集APIが十分になるまで作成UIを公開しない。既存データの読み込み互換性は維持する。
- 構造と制約からTeX形式の入力・制約表示とランダムサンプルを生成する。
- 編集途中の未完成な構造や制約も第一級の状態として保持する。
- 意味上の判断、編集の合法性、既定値、変換はRust側に置き、Web UIは投影結果の表示と宣言的なActionの発行に集中する。
- Rustのドメイン実装は`cp-ast-ecosystems/`サブモジュールからWASMとしてビルドする。
- インタラクティブ問題の完全対応は現時点の対象外。
- 一般述語を含む複雑な制約の充足は保証せず、生成はベストエフォートになる場合がある。

## Brand Commitments

- UIの表示言語は日本語とする。
- 「Random Test Creator」は仮称であり、固定された製品名として扱わない。
- AtCoderの表示形式は維持するが、AtCoder専用製品とは位置づけない。
- 確定したロゴやブランドアセットは現時点でない。

## Evidence on Hand

- `cp-ast-ecosystems/doc/design/final-design.md`に、AST、制約、投影、操作、サンプル生成の設計と調査結果がある。
- `cp-ast-ecosystems/doc/view/problem-user-flows.md`に、配列、グリッド、木、クエリ列などの具体的な操作フローがある。
- `cp-ast-ecosystems/doc/survey/`に、AtCoderを中心とした問題形式の調査とカバレッジ資料がある。
- `tests/e2e/`に、配列、グリッド、グラフ、木、クエリ、複数テストケース、可変長配列などのブラウザ操作テストがある。
- 実在する利用者の声、導入事例、公開ベンチマーク、確定した価格・ライセンス情報は確認できていないため、今後の表現で捏造しない。

## Product Principles

1. 単一のASTを正本にし、編集・表示・生成の整合性を保つ。
2. 問題作成者と問題回答者のどちらも、構造と制約を段階的に組み立てられるようにする。
3. 未完成な編集状態を壊れた状態として捨てず、次に必要な操作が分かる有意味な状態として扱う。
4. 対象サイトを限定せず、AtCoderの表示形式との互換性を維持する。
5. ドメイン上の規則はRustに集約し、UIに重複した意味判断を持ち込まない。

## Open Decisions

- 正式な製品名とブランド識別要素。
- 問題を解いている利用者向けに、ランダム入力生成以外のどこまでを支援するか。
